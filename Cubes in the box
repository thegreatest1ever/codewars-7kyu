function f(x,y,z){
  if (x === 1 || y=== 1 || z === 1) {
    return x * y * z 
  }
  return x* y * z + f(x-1,y-1,z-1)
}
/*
const f = (x,y,z) => Array.from({length: Math.min(x,y,z)}, (_,i) => (x-i)*(y-i)*(z-i)).reduce((a,c)=> a+c);
*/
